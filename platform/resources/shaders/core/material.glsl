struct mtl_flat_color_t {
    vec3 ambient;
    vec3 diffuse;
    vec3 specular;
    float shininess;
};

struct mtl_flat_texture_t {
    vec3 ambient;
    vec3 diffuse;
    vec3 specular;
    float shininess;
};
